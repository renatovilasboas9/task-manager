/**
 * Task Manager Service Layer Exports
 * 
 * This module exports the service layer components for the task manager domain.
 * The service layer contains business logic and orchestrates operations between
 * the UI layer and the repository layer.
 */

export { TaskService } from './TaskService'